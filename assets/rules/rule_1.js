/*******************
    - File name to always be rule_<group_id>.js
    -   One js file for every group with a rule to follow
    - Main function name to always be rule_<group_id>()
    - Function to always return a status color of:
    -   "green", "yellow", "red" 
********************/

function rule_1() {
    // Defining the data variables for this group
    // var alpha = coreData.query_groups[0].queries[0].esData;


    return "green";
    // Set the conditions for choosing a status to return
    /* if (  ) {
        return "red";
    } else if (  ) { 
        return "yellow"
    } else {
        return "green";
    } */ 
}